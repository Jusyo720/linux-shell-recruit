# 学习Linux过程中的笔记

- Linux中表示路径用/，Windows用\。
- Linux的根目录为/。
***
- Linux命令的基本格式：**command -options parameter** ，其中后两项可选，非必填。

    - 第二项可填命令选项，以此控制行为细节。

   - 第三项可填命令的参数，多用于命令的指向目标。

***
## Linux基本命令
1.ls命令：`lsa`表示列出当前用户主目录下的文件和文件夹，后面可加`-a`（表示所有文件，包括以`.`开头的隐藏文件）；`-l`（表示以列表的方式展示内容，并展示更多信息）；
2.