# 学习Linux过程中的笔记

- Linux中表示路径用/，Windows用\。
- Linux的根目录为/。
***
- Linux命令的基本格式：**command -options parameter** ，其中后两项可选，非必填。

    - 第二项可填命令选项，以此控制行为细节。

   - 第三项可填命令的参数，多用于命令的指向目标。

***
## Linux基本命令
### 01_project_hunt
- `ls`命令：全称**list**，表示列出当前用户主目录下的文件和文件夹，后面可加:  
    - `-a`（表示所有文件，包括以`.`开头的隐藏文件）；
    - `-l`（表示以列表的方式展示内容，并展示更多信息）；
    - `-lah`（一次性显示所有文件的详细信息和人类可读大小）；  
    - `-R`（递归地列出目录及其所有子目录下的内容）等。
- `pwd`命令：全称**Print Working Directory**，意思是“打印当前工作目录”，用于提示用户“你现在在文件系统的哪个位置”。  
- `cd`命令：全称**Change Directory**，用于切换当前工作目录的命令。
    - `cd ~`：当前用户的**家目录**；
    - `cd ..`：上一级目录；`cd ../..`：上两级目录；
    - `cd -`：上一次所在的目录；
    - `cd/`：根目录；
    - `cd /usr/local`：绝对路径；
    - `cd Documents`：相对路径，进入当前目录下的Documents；
    - `cd ~/Downloads`：进入家目录下的Downloads。
- 绝对路径与相对路径：  
    - 绝对路径：完整、精确的地址，从根目录`/`开始写起；  
    - 相对路径：相对于“你当前所在目录”的路径，通常以`.`或文件名开头。
- `find`命令：基础语法`find 路径 -条件 条件值`。[^1]  
- `grep`命令：基础语法`grep 要找的词 文件名`。[^2]  
    - grep -n：显示行号；
    - grep -i：忽略大小写。
- `cat`命令：全称**Concatenate**，在实际使用中用来查看文件内容或创建新文件，作用是把一个文件的内容**直接打印到屏幕上**。
    - `cat 文件名`：打印文件中的内容；
    - 创建一个新文件并写入内容：输入`cat > 文件名`回车，然后敲入文件内容，最后按Ctrl+C退出。[^3]
***
### 02_missing_command
- `chmod`命令：全称**Change Mode**。只有文件的所有者或root用户才能执行此命令。
    - ls -l命令执行后可以查看文件当前的权限状态，输出格式为`rwxrwxrwx`，其中前三位表示文件所有者权限，中间三位代表所有者权限，最后三位代表其他用户权限。
    - 三类用户：**u**（所有者user）；**g**（所属组group）；**o**（其他用户others）；**a**（所有用户all，即**ugo**）。
    - 三种权限：**r**（读，对应数字**4**）；**w**（写，对应数字**2**）；**x**（执行，对应数字**1**）。
    - 操作符：**+**（添加权限）；**-**（移除权限）；**=**（精确设置并清除未指定的权限）。
    - 语法格式：
        - 符号模式：`chmod [用户类别][操作符][权限]文件名`。[^4]
        - 数字模式：`chmod [三位八进制数字] 文件名`。
- `./`：表示当前目录。
- `command -v`：查询某个命令或程序在系统中具体的存储路径（绝对路径）。
- `export`：将Shell变量升级为环境变量，从而让当前终端启动的所有子程序都能继承并使用它。

[^1]: 例如`find  -name "check.sh"`意为在当前目录里找一个名为`check.sh`的文件。  
[^2]：例如`grep "install" README.md`意为在README.md中把包含"install"的那一行打印出来。  
[^3]：`>`表示覆盖写入，清空替换文件中原有内容。  
[^4]：例如`chmod u+x script.sh`（给所有者添加执行权限，常用于让脚本可运行）；`chmod a=r file.txt`（将所有用户的权限精准设置为只读）。
