# 学习Linux过程中的笔记

- Linux中表示路径用/，Windows用\。
- Linux的根目录为/。
***
- Linux命令的基本格式：**command -options parameter** ，其中后两项可选，非必填。

    - 第二项可填命令选项，以此控制行为细节。

   - 第三项可填命令的参数，多用于命令的指向目标。

***
## Linux基本命令
- `ls`命令：`ls`表示列出当前用户主目录下的文件和文件夹，后面可加:  
    - `-a`（表示所有文件，包括以`.`开头的隐藏文件）；
    - `-l`（表示以列表的方式展示内容，并展示更多信息）；
    - `-lah`（一次性显示所有文件的详细信息和人类可读大小）等。  
- `pwd`命令：全称Print Working Directory，意思是“打印当前工作目录”，用于提示用户“你现在在文件系统的哪个位置”。  
- `cd`命令：全称Change Directory，用于切换当前工作目录的命令。
    - `cd ~`：当前用户的家目录；
    - `cd ..`：上一级目录；`cd ../..`：上两级目录；
    - `cd -`：上一次所在的目录；
    - `cd/`：根目录；
    - `cd /usr/local`：绝对路径；
    - `cd Documents`：相对路径，进入当前目录下的Documents；
    - `cd ~/Downloads`：进入家目录下的Downloads。
- 绝对路径与相对路径：  
    - 绝对路径：完整、精确的地址，从根目录`/`开始写起；  
    - 相对路径：相对于“你当前所在目录”的路径，通常以`.`或文件名开头。
- `find`命令：基础语法`find 路径 -条件 条件值`